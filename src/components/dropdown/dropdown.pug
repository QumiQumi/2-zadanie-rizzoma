mixin dropdown(options="")
    .dropdown
        .dropdown__name Сколько
        .dropdown-list
            each item in options.items
                div(value=item.value).dropdown-list__item #{item.text}
